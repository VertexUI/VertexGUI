import CustomGraphicsMath
import WidgetGUI

public struct Style {
    public static let PrimaryColor = Color(40, 60, 200, 255)
    public static let Theme = DefaultTheme(mode: .Dark, primaryColor: Style.PrimaryColor)
}