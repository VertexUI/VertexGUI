import WidgetGUI
import GfxMath

// TODO: maybe add AppTheme.defaultStyles -> add globally
public enum AppTheme {
    public static let primaryColor = Color(230, 230, 103, 255)
    public static let backgroundColor = Color(10, 24, 36, 255)
    public static let listItemDividerColor = backgroundColor.darkened(80)
}