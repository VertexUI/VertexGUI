// TODO: maybe instead of using ObservableValues, use States for stateful widgets and keep them via automatic comparison on tree updates + keys where not possible
public struct ObservableValue<V> {
    
}