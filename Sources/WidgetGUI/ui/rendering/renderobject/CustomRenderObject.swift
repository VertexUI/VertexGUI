import VisualAppBase
/*
public struct CustomRenderObject {
    public typealias RenderCallback = (_ renderer: Renderer) throws -> Void

    private var renderCallback: RenderCallback

    public init(_ renderCallback: @escaping RenderCallback) {
        self.renderCallback = renderCallback
    }

    public func render(renderer: Renderer) throws {
        try renderCallback(renderer)
    }
}*/