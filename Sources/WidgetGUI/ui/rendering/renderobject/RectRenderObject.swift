import VisualAppBase
import CustomGraphicsMath

/*
public struct RectRenderObject: RenderObject {
    private var rect: DRect
    private var style: RenderStyle

    public init(_ rect: DRect, style: RenderStyle) {
        self.rect = rect
        self.style = style
    }

    public func render(renderer: Renderer) {
        // TODO: change Renderer to use simple calls, like fill(), stroke()
        // TODO: maybe need a RenderObjectRenderer which translates the things most effectivly on different platforms later
        //renderer.rect()
    }
}*/